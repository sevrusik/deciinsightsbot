# Развертывание Dice of Isight Bot на Synology NAS 218+

Инструкция по развертыванию Telegram бота на Synology NAS без использования платных облачных сервисов.

## Требования

- Synology NAS 218+ с DSM 7.0 или выше
- Пакет Docker установлен через Package Center
- Доступ по SSH к NAS (опционально, для командной строки)
- Telegram Bot Token (получить у @BotFather)
- OpenAI API Key

## Подготовка NAS

### 1. Установка Docker

1. Откройте **Package Center** на вашем NAS
2. Найдите и установите пакет **Docker**
3. Дождитесь завершения установки

### 2. Создание директории для проекта

Через **File Station** или SSH создайте директорию:
```
/volume1/docker/dice-bot/
```

### 3. Загрузка файлов проекта

Скопируйте все файлы проекта в созданную директорию на NAS:

```bash
# Если используете scp с вашего Mac
scp -r "/Volumes/KINGSTON/Projects/Dice of Isight BOT/"* admin@YOUR_NAS_IP:/volume1/docker/dice-bot/
```

Или используйте **File Station** для загрузки файлов через веб-интерфейс.

## Настройка переменных окружения

### 1. Создание .env файла

В директории `/volume1/docker/dice-bot/` создайте файл `.env` со следующим содержимым:

```env
# Telegram Bot Token (получить у @BotFather)
BOT_TOKEN=your_telegram_bot_token_here

# OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# Admin IDs (Telegram user IDs через запятую)
ADMIN_IDS=123456789,987654321

# База данных (не меняйте для NAS)
DATABASE_URL=sqlite:////app/data/dice_bot.db

# Режим работы (для NAS оставьте пустым - будет polling)
RENDER_EXTERNAL_URL=
```

### 2. Получение необходимых токенов

**Telegram Bot Token:**
1. Напишите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

**OpenAI API Key:**
1. Зайдите на https://platform.openai.com/api-keys
2. Создайте новый API ключ
3. Скопируйте ключ (он показывается только один раз!)

**Ваш Telegram ID:**
1. Напишите @userinfobot в Telegram
2. Он пришлет ваш ID
3. Добавьте его в ADMIN_IDS

## Развертывание через Docker UI

### Вариант 1: Через Docker UI (рекомендуется для начинающих)

1. Откройте приложение **Docker** на NAS
2. Перейдите в раздел **Image**
3. Нажмите **Add** → **Add from file**
4. Выберите файл `Dockerfile` или используйте терминал для сборки

### Вариант 2: Через SSH (рекомендуется)

1. Подключитесь к NAS по SSH:
```bash
ssh admin@YOUR_NAS_IP
```

2. Перейдите в директорию проекта:
```bash
cd /volume1/docker/dice-bot
```

3. Создайте директории для данных:
```bash
mkdir -p data logs
```

4. Соберите Docker образ:
```bash
sudo docker-compose build
```

5. Запустите контейнер:
```bash
sudo docker-compose up -d
```

## Управление ботом

### Запуск бота
```bash
sudo docker-compose up -d
```

### Остановка бота
```bash
sudo docker-compose down
```

### Перезапуск бота
```bash
sudo docker-compose restart
```

### Просмотр логов
```bash
sudo docker-compose logs -f
```

### Обновление бота
```bash
# Остановите бот
sudo docker-compose down

# Загрузите новые файлы проекта
# (используйте scp или File Station)

# Пересоберите образ
sudo docker-compose build

# Запустите бот
sudo docker-compose up -d
```

## Автозапуск при перезагрузке NAS

Docker контейнер настроен на автоматический запуск (`restart: unless-stopped` в docker-compose.yml).

Также можно настроить через Task Scheduler в DSM:

1. Откройте **Control Panel** → **Task Scheduler**
2. Create → **Triggered Task** → **User-defined script**
3. General:
   - Task: `Start Dice Bot`
   - User: `root`
   - Event: `Boot-up`
4. Task Settings:
   ```bash
   cd /volume1/docker/dice-bot
   docker-compose up -d
   ```
5. Сохраните задачу

## Мониторинг и обслуживание

### Проверка состояния
```bash
sudo docker-compose ps
```

### Использование ресурсов
```bash
sudo docker stats dice-of-isight-bot
```

### Очистка логов (если занимают много места)
```bash
sudo docker-compose logs --tail=100 > latest_logs.txt
sudo truncate -s 0 logs/*.log
```

### Резервное копирование базы данных
```bash
# Создайте backup директорию
mkdir -p /volume1/docker/dice-bot/backups

# Скопируйте базу данных
sudo cp /volume1/docker/dice-bot/data/dice_bot.db /volume1/docker/dice-bot/backups/dice_bot_$(date +%Y%m%d).db
```

## Решение проблем

### Бот не запускается

1. Проверьте логи:
```bash
sudo docker-compose logs
```

2. Убедитесь, что `.env` файл правильно заполнен
3. Проверьте, что Docker контейнер запущен:
```bash
sudo docker ps
```

### Ошибки с базой данных

Проверьте права доступа:
```bash
sudo chmod 777 /volume1/docker/dice-bot/data
```

### Бот не отвечает в Telegram

1. Проверьте интернет-соединение NAS
2. Убедитесь, что используется polling режим (RENDER_EXTERNAL_URL пустой)
3. Проверьте, что BOT_TOKEN правильный

### Высокое использование CPU/RAM

1. NAS 218+ имеет ограниченные ресурсы
2. Рассмотрите использование более легкой модели AI (в config.py измените AI_MODEL на "gpt-3.5-turbo")
3. Ограничьте количество одновременных запросов

## Преимущества развертывания на NAS

✅ **Бесплатно** - нет ежемесячных платежей за хостинг
✅ **Полный контроль** - все данные хранятся локально
✅ **Приватность** - логи и база данных на вашем устройстве
✅ **Надежность** - NAS работает 24/7
✅ **Резервное копирование** - встроенные инструменты Synology

## Важные замечания

⚠️ **Порты**: Не нужно открывать порты на роутере - бот работает через polling
⚠️ **Производительность**: NAS 218+ имеет ARM процессор - это нормально для Python ботов
⚠️ **Обновления**: Регулярно обновляйте зависимости командой `pip install -U -r requirements.txt` в контейнере
⚠️ **Безопасность**: Не публикуйте .env файл в публичных репозиториях

## Дополнительные возможности

### Мониторинг через Telegram

Бот уже имеет команду `/stats` для администраторов, которая показывает статистику использования.

### Уведомления при ошибках

Можно настроить отправку критических ошибок администраторам через Telegram.

### Регулярные бэкапы

Используйте Hyper Backup в DSM для автоматического резервного копирования директории `/volume1/docker/dice-bot/`.

## Контакты и поддержка

Если возникли проблемы:
1. Проверьте логи: `sudo docker-compose logs -f`
2. Изучите документацию Synology Docker
3. Проверьте состояние сервисов Telegram API

---

**Готово!** Ваш бот теперь работает на вашем NAS без дополнительных расходов! 🎲✨
