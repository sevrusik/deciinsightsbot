# Деплой Dice of Isight Bot на Render.com

## Подготовка

### 1. Загрузите код на GitHub

```bash
cd "/Volumes/KINGSTON/Projects/Dice of Isight BOT"
git init
git add .
git commit -m "Initial commit - Dice of Isight Bot"
git remote add origin https://github.com/sevrusik/deciinsightsbot.git
git branch -M main
git push -u origin main
```

## Деплой на Render.com

### Шаг 1: Регистрация
1. Зайдите на [render.com](https://render.com)
2. Зарегистрируйтесь через GitHub

### Шаг 2: Создание сервиса
1. Нажмите **"New +"** → **"Web Service"**
2. Подключите репозиторий `sevrusik/deciinsightsbot`
3. Render автоматически обнаружит `render.yaml`
4. Или настройте вручную:
   - **Name**: `dice-of-isight-bot`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`
   - **Instance Type**: `Free`

### Шаг 3: Переменные окружения

В разделе **Environment** добавьте переменные из вашего `.env` файла:

| Key | Value | Где взять |
|-----|-------|-----------|
| `BOT_TOKEN` | Ваш токен | [@BotFather](https://t.me/BotFather) в Telegram |
| `OPENAI_API_KEY` | Ваш API ключ | [OpenAI Platform](https://platform.openai.com/api-keys) |
| `PYTHON_VERSION` | `3.11.0` | (опционально) |

**Важно**: НЕ коммитьте файл `.env` в Git! Он уже в `.gitignore`

### Шаг 4: Деплой
1. Нажмите **"Create Web Service"**
2. Render автоматически:
   - Склонирует репозиторий
   - Соберет Docker образ
   - Установит зависимости
   - Запустит бота

### Шаг 5: Проверка
1. Откройте **Logs** чтобы увидеть:
   ```
   🎲 Dice of Isight Bot запускается...
   ```
2. Протестируйте бота в Telegram

## Автоматические обновления

Каждый `git push` автоматически запускает деплой:

```bash
# Внесите изменения в код
git add .
git commit -m "Update bot features"
git push
```

Render автоматически:
- Обнаружит изменения
- Пересоберет проект
- Перезапустит бота

## Важные моменты

### ⚠️ Ограничения бесплатного тарифа:
- Бот **усыпает** после 15 минут неактивности
- При первом запросе пробуждается (~30 сек)
- 750 часов работы в месяц
- Для постоянной работы нужен **Hobby план** ($7/мес)

### 💾 База данных SQLite
- **Проблема**: SQLite файл удаляется при каждом деплое
- **Решение для продакшена**: PostgreSQL

#### Как добавить PostgreSQL (бесплатно):
1. В Render: **New +** → **PostgreSQL**
2. Name: `dice-bot-db`
3. Free tier (90 дней, потом $7/мес)
4. Скопируйте **Internal Database URL**
5. Добавьте в Environment Variables:
   ```
   DATABASE_URL = postgresql://...
   ```
6. Обновите `database.py`:
   ```python
   import os

   DATABASE_URL = os.getenv('DATABASE_URL', 'sqlite:///dice_bot.db')
   engine = create_engine(DATABASE_URL)
   ```

### 📊 Мониторинг

- **Логи**: Dashboard → Logs
- **Метрики**: Dashboard → Metrics
- **Уведомления**: Settings → Notifications → Deploy notifications

### 🔒 Безопасность

✅ `.gitignore` уже настроен:
- `.env` файлы не попадут в Git
- `config.py` защищен
- Базы данных и логи исключены

## Troubleshooting

### Бот не отвечает
1. Проверьте логи в Render Dashboard
2. Убедитесь что `BOT_TOKEN` правильный
3. Проверьте что бот запущен (не усыплен)

### "Module not found"
- Проверьте `requirements.txt`
- Пересоберите: **Manual Deploy** → **Clear build cache & deploy**

### База данных пустая после деплоя
- Это нормально для SQLite
- Используйте PostgreSQL для постоянного хранения

### Бот усыпает слишком часто
- Апгрейд до **Hobby плана** ($7/мес)
- Или используйте VPS (Hetzner €4.5/мес)

## Альтернативы

Если бесплатный Render не подходит:

| Платформа | Цена | Плюсы | Минусы |
|-----------|------|-------|--------|
| Render Hobby | $7/мес | Не усыпает | Платно |
| Railway | $5 кредитов | Простой | Кредиты кончаются |
| VPS Hetzner | €4.5/мес | Полный контроль | Нужно настраивать |
| Fly.io | Free tier | Не усыпает | Сложная настройка |

## Полезные команды

```bash
# Проверить статус Git
git status

# Посмотреть логи локально
tail -f dice_bot.log

# Проверить переменные окружения
cat .env

# Тестировать бота локально
python main.py
```

## Следующие шаги

1. ✅ Деплой на Render
2. ⏳ Настроить PostgreSQL (для продакшена)
3. ⏳ Настроить мониторинг и алерты
4. ⏳ Добавить CI/CD тесты перед деплоем
